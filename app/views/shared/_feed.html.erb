<% if @feed_items.any? %>
  <ol class="microposts" data-pagination="true">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
  <p data-pagination-loading="true" class="hide" >Loading more microposts...</p>
<% end %>